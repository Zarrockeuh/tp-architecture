apiVersion: v1
kind: Service
metadata:
    name: "k8sclusterdev-368a"
    labels:
        app: "k8sclusterdev-368a"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "k8sclusterdev-368a"